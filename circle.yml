machine:
  environment:
    COVERAGE: 1

general:
  artifacts:
    - coverage

test:
  override:
    - bin/rubocop
    - bin/rspec -r rspec_junit_formatter --format doc --format RspecJunitFormatter -o "${CIRCLE_TEST_REPORTS}/rspec/junit.xml"
